# File Encryption Secret (MUST be kept secure, 32+ characters)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FILE_ENCRYPTION_SECRET=your-secure-random-secret-here

# Path to store encrypted files (absolute path recommended)
ENCRYPTED_FILES_PATH=/path/to/secure/storage

# Database connection (customize as needed)
# DATABASE_URL=postgresql://postgres:postgres@localhost/argus_ai_secure_poc
